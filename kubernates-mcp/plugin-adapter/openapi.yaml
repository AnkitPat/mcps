openapi: 3.0.1
info:
  title: Kubernetes Plugin
  version: 1.0.0
servers:
  - url: http://localhost:3000
paths:
  /deployments:
    get:
      summary: List deployments
      operationId: listDeployments
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
